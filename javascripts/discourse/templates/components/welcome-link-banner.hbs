
{{#unless hideStaff}}
  {{#unless (and (theme-setting 'hide_on_mobile') site.mobileView)}}
    {{#if (and showTrust showHere)}}
      <div class="welcome-link-banner-wrapper">
        <div class="wrap welcome-link-banner">
          <div class="welcome-wrapper">
               <h2>{{html-safe (theme-i18n "meta_banner.welcome")}}</h2>
              <p id="intro-ban">{{html-safe (theme-i18n "meta_banner.subtitle")}}</p>
              <p>Les équipes de LNF n'interviennent pas prioritairement sur le forum qui
              est un lieu d'échange pour la communauté. Pour poser une question en lien avec la formation
              utiliser le ticketing.</p>
           </div> 
         </div>              
      </div>
    {{/if}}
  {{/unless}}
{{/unless}}


